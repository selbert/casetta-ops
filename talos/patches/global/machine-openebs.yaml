machine:
  kubelet:
    extraMounts:
      - destination: /var/local
        type: bind
        source: /var/local
        options: ["bind", "rshared", "rw"]
  sysctls:
    vm.nr_hugepages: "2048"
  nodeLabels:
    openebs.io/engine: "mayastor"
