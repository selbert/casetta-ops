machine:
  kubelet:
    extraMounts:
      - destination: /var/openebs
        type: bind
        source: /var/openebs
        options: ["bind", "rshared", "rw"]
  sysctls:
    vm.nr_hugepages: "1024"
  nodeLabels:
    openebs.io/engine: "mayastor"
